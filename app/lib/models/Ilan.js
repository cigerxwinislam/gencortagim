// Bismillahirahmanirahim



import mongoose, { Schema, models } from "mongoose";

const userSchema = new Schema(
  {
    name: {
      type: String,
      required: true,
    },
    email: {
      type: String,
      required: true,
    },
    password: {
      type: String,
      required: true,
    },
  },
  { timestamps: true }
);

const Siparis = models.Ilan || mongoose.model("Ilan", userSchema);
export default Ilan;
